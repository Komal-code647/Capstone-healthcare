version: "3.9"

services:

  healthcare-assistant:

    build: .

    container_name: healthcare_assistant

    ports:
      - "8501:8501"
      - "8000:8000"

    env_file:
      - .env

    restart: always
